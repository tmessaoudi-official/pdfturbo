# P2 backlog (QA sweep 2026-06-23) — fix plan

## Decisions Log
- [2026-06-23] AGREED: Fix ALL 34 P2s from the sweep, grouped into 4 themes, TDD, one commit per theme.
- [2026-06-23] AGREED ORDER + why: (1) **Security & input hygiene** — independent, high-value, near-zero blast radius; bank quick wins first. (2) **Undo/redo correctness** — reuses the warm form-fill flush pattern; small command-layer fixes. (3) **Reliability & silent-failure** — highest user-impact P2 (recentColors startup crash) + leaks + feedback. (4) **Raster-export parity** — LAST: most involved (pixel tests), highest regression risk, touches the rasterizer just rewritten for the redaction-crop fix → build on a stable base. Sequence is risk-ascending, context-local, build-on-stable.

## Theme 1 — Security & input hygiene (3)
- MD hyperlink URL: percent-encode + allowlist schemes (http/https/mailto) — `flowDocWriters.ts` (and DOCX ExternalHyperlink). Drop/neutralize javascript:/data:.
- CSV formula injection: prefix `'` for cells starting `= + - @ \t \r` in `csvField` — `tableExtract.ts`/exportService.
- wordPaste images: restrict `data:` to raster allowlist, consider size ceiling, reject/document remote https: — `wordPaste.ts` `_imgIsUsable`.

## Theme 2 — Undo/redo correctness (3)
- In-flight overlay-text edit discarded on undo → `_cancelPendingTextEdit` should FLUSH (mirror the form-fill fix). `undoRedoController.ts`.
- MacroCmd no atomic rollback if a child throws → roll back applied prefix. `macroCmds.ts`.
- BulkDeleteCmd.undo loses z-order → re-insert at captured indices. `inkCmds.ts`/wherever BulkDeleteCmd lives.

## Theme 3 — Reliability & silent-failure feedback
- recentColors corrupt-localStorage crash → Array.isArray guard in `recentColors.ts` read().
- pdf.js render docs leaked in downloadPageAsImage + rasterizePageWithRedactions → loadingTask.destroy() in finally.
- Eraser bbox over-delete → distance-to-polyline test. `eraserGeometry.ts`.
- Image atob abort in DOCX writer → try/catch per image. `flowDocWriters.ts`.
- Per-element bake silent drop → name the element type in the toast. `exportService.ts`.
- Small/low-zoom widget drop → feedback / clamp. `formFieldOverlay.ts`.
- onPageStructureChange drops concurrent refresh → trailing-edge coalesce.
- (triage the rest of the 34 as encountered)

## Theme 4 — Raster-export parity (5) — LAST
- Raster text bake drops Slice-2 attrs + alignment + sub/super + decorations vs vector bake → apply in the canvas loop. `exportPipeline.ts`.
- Watermark/Bates mis-anchor on /Rotate'd page → map anchor via tp(). `exportPipeline.ts`.
- Decoration-remove hard tolerance → document or symmetric-overlap fallback. `contentStreamEditor.ts`.
- Each guarded by a real-Chrome pixel test.

## STATUS (2026-06-23 — ALL THEMES NOW DONE)
- **Theme 1 DONE** `26ac79c` (CSV/MD/Word-paste injection guards).
- **Theme 2 DONE** `4aa1ac3` (text-edit flush, MacroCmd rollback, BulkDelete z-order).
- **Theme 3 DONE** — `b5aeabe` (recentColors crash, pdf.js leak, atob abort) + `e7abe61`
  (eraser bbox over-delete → swept-region/capsule clip model, real-PointerEvent browser
  guard + live visual QA in `qa-shots/eraser-2026-06-23/`) + `833ffad` (per-element toast
  type, small-widget clamp-not-drop, onPageStructureChange trailing-edge coalesce).
- **Theme 4 DONE** `9e7cdae` — raster-export text parity (`src/export/rasterText.ts`:
  align/stroke/Tc/Tz/sub-sup/underline/strike) + Bates visual-corner anchor on rotated
  pages (byte-identical at rot=0/crop). Pixel guards: `raster-text-parity`, `bates-rotation`.
  Watermark left page-relative (tiled full coverage — cosmetic-only on rotation, documented).
- All P1s (4/4) DONE earlier this session: `7b63c2f`, `20a2448`, `575e2a5`.
- Everything committed, UNPUSHED (user pushes manually). Latest gate: jsdom 1947+2/169 green;
  browser regression batch green. Session commits: a6c9ab2..833ffad.

## Gate per theme
type-check + oxlint + jsdom + relevant real-Chrome browser tests; one commit per theme; mark report P2s fixed.
