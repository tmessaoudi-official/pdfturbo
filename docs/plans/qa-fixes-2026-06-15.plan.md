# QA-Fixes 2026-06-15 Plan

Source: live QA-sweep report `~/.claude/projects/-stack-projects-prsnl-pdfturbo/qa-sweep/2026-06-15-qa-sweep.md`.
Scope (user, 2026-06-15): "Everything" — QA findings → queued DOCX fidelity → signing S8/S9. Execution: **TDD, gate each P1/P2 fix**.

## Decisions Log
- [2026-06-15] AGREED: Full QA sweep done (8 areas + convergence). No P0; both P0 security fixes (rotated-redaction, AES-256 lock) verified working live.
- [2026-06-15] AGREED: Fix order = QA findings (severity) → fidelity → S8/S9. TDD with per-fix gate on P1/P2.
- [2026-06-15] AGREED: Execution = browser-e2e R2 → commit R2 → continue; "challenge me" active throughout.
- [2026-06-15] DONE: R2 (P1) fixed + committed `3aef26f` (jsdom + browser test). Plan committed `83fe76a`.
- [2026-06-15] AGREED: R1 DEFERRED to P3 — after R2, edit-text no longer uses the textLayer; remaining impact (selection/search-highlight on rotated pages) is minor and the fix is pdf.js-v6-TextLayer-integration-risky. Mechanism pinned: textLayer.render gets a correct rotated viewport but pdf.js v6 renders spans unrotated anyway.
- [2026-06-15] DONE: S-FLOW (P2) implemented via TDD. Extracted `isPdfSigned()` + cert-free `PdfSigner.preflight(bytes,page,rect)`; `pdfTurboApp.signPdf` assembles ONCE → preflights BEFORE cert-gen → bails on placement/already-signed/assembly error with NO orphan cert download; generate-mode password no longer wiped in `finally` (only `closeSignModal`); `signingHandler.sign(form, preassembled?)` signs the same bytes. 9 new jsdom tests (`tests/signing/preflight.test.ts`); full suite 1046+2; tsc/oxlint clean. Live-verified both paths (off-page: 0 downloads/error/pw-retained; valid: p12+pem+signed.pdf, modal closed). CLAUDE.md updated. NOT yet committed (awaiting per-fix gate).
- [2026-06-15] AGREED: next-work order = Searchable-OCR → Sanitize → PII(regex) → FS Access → Signing S8/S9; transformers.js NER deferred to a separate measured spike. Each item starts with its validation spike. Fidelity/QA fix queue EMPTY. (Session paused here — user closing; nothing built yet, recorded for resume.)
- [2026-06-15] FINDING (verify-before-fix #4): "DOCX #2 — Path-3 fill-color canvas-sample" was ALREADY SHIPPED in `d7879fb` (Sprint 4). `resolveRedrawColor(styleColor, fillColorRaw, fallbackColor)` (4-tier: style > parsed rg/g/k > canvas sample > black) + `replaceTextAt(...fallbackColor)` + handler passes `sampledFallback = hexToRgb01(overlayContext.textColor)` (textEditHandler:506-507). 6 unit tests green (`tests/utils/contentStreamColor.test.ts`, incl. the exact scn/Separation bug case). The `02-trueedit-matrix.md` "Reachable Gaps" section is STALE on ALL THREE: Gap 1 (TJ kerning distribute) DONE `:585-592`, Gap 2 (this) DONE, Gap 3 (exponent) DONE `:84-87`. AGREED: add a real-Chrome regression forcing Path-3 (Helvetica + `scn` fill + edit to non-ASCII `é` → assert redrawn glyph ≠ #000000) to pin the canvas-sample seam, THEN correct the stale docs.

- [2026-06-15] DONE: Searchable-OCR **spike PASSED** (item #1, pure-mechanism scope). New `src/ocr/searchableTextLayer.ts` — `wordToTextPlacement` (OCR-px top-left → PDF-pt bottom-left) + `buildInvisibleTextLayerOps` (`BT·Tr(3)·Tf·Tm·Tj·ET` per word, arabicOverlay `pushOperators` pattern). Proven end-to-end in real Chrome: invisible text is selectable via pdf.js `getTextContent` AT the right coords (`transform[4]/[5]` within 0.5pt) AND rasterizes 0 dark pixels. 7 jsdom + 1 browser test; suite 1056+2, tsc/oxlint clean; new files only (no app wiring yet). Verdict: `docs/reviews/2026-06-15-searchable-ocr-spike-verdict.md`. Spec: `docs/superpowers/specs/2026-06-15-searchable-ocr-spike-design.md`. NOT committed (awaiting user). NEXT: build the full feature (ocrHandler mode switch + SourcePdf swap/undo + UI toggle + non-Latin font handling).

- [2026-06-16] AGREED: Build searchable-OCR FULL feature now (context hot, mechanism de-risked). Replaced "spike-all-first" with: finish #1 full, then thin-slice TDD builds for #2-#4 (no throwaway spikes — they're already known-feasible; only OCR + transformers.js carried real risk). transformers.js NER stays a separate measured spike.
- [2026-06-16] AGREED: Non-Latin = **Include Arabic (all 8 advertised langs)**. Latin-7 (eng/fra/deu/spa/ita/por/nld) via Helvetica/WinAnsi; Arabic via the already-bundled Noto Naskh, emitted in LOGICAL order (NO CID reversal — invisible text needs search-matchable Unicode via ToUnicode, not visual RTL placement). Words that are neither Arabic nor WinAnsi-safe (`hasNonWinAnsi`) are skipped from the layer. UI default = "Searchable layer".

## RESUME STATE (post-compact entry point)
- HEAD = the DOCX #2 e2e-guard commit (push pending — user pushes manually). QA report: `~/.claude/projects/-stack-projects-prsnl-pdfturbo/qa-sweep/2026-06-15-qa-sweep.md`.
- **DOCX #2 DONE — the substantive fix was ALREADY SHIPPED in `d7879fb` (Sprint 4); verify-before-fix #4.** `resolveRedrawColor` (style > parsed rg/g/k > canvas `fallbackColor` > black) + `replaceTextAt(…,fallbackColor)` + handler `sampledFallback`. This session ADDED the missing e2e proof: `tests/browser/truedit-spot-color.browser.test.ts` (Separation fixture → forces Path-3 via Helvetica `é` edit → asserts chromatic redraw + black control). Regression-verified (test fails when the fallback tier is disabled). NO src change. Stale docs corrected: `02-trueedit-matrix.md`, `scorecard-trueedit.md`, CLAUDE.md, this file. **All three `02-trueedit-matrix.md` "reachable gaps" are now done** (Gap 1 TJ-kerning `:585-592`, Gap 2 this, Gap 3 exponent `:84-87`).
- **NEXT — AGREED SEQUENCE (2026-06-15, user picked "recommended order"):**
  1. **Searchable-OCR layer** — ✅ DONE (spike + FULL feature, 2026-06-16). `ocrHandler.run(lang,mode)` `'visible'|'searchable'`; searchable swaps source bytes via `_applySourcePdfEdit` (undoable); `ocrModeSelect` UI default "Searchable layer". Latin-7 exact-searchable; **Arabic = documented partial** (selectable real Arabic Unicode, exact full-word search imperfect — clean-ToUnicode PoC tried+rejected: per-codepoint encoding caused RTL reversal in pdf.js getTextContent). Rotated pages warn+skip. 14 jsdom + 2 browser tests; suite 1063+2 / 36 browser, tsc+lint clean. NOT committed. Follow-ups (not queued): rotated-page mapping, Arabic exact-search ToUnicode CMap, Tz width-fit.
  2. **Sanitize / metadata-scrub** — spike: load a PDF with `/Info` + `/OpenAction` JS, scrub (XMP, /Info, embedded JS, attachments), re-open, assert gone. pdf-lib catalog edits, no new deps. Effort S-M.
  3. **PII detector (regex/dictionary MVP)** — IBAN/email/phone → one-click redaction. NO model. Effort M.
  4. **FS Access API** — `showOpenFilePicker`/`showSaveFilePicker`, feature-detected (Chromium-only), fallback to current download path. Effort S.
  5. **Signing S8** (/Contents right-size hex slot) + **S9** (leaf-cert selection from chain).
  - **DEFERRED (separate measured spike, NOT in the sequence): transformers.js NER** — the only feasibility-risk item (model MB + WASM vs WebGPU latency). Measure size/latency BEFORE committing; the regex MVP (#3) ships first and stands alone. Local-AI is GRDF-policy-safe by construction (in-browser, nothing transmitted).
  - Each item starts with its named validation spike ("research+test before commitment"). Research: `docs/reviews/2026-06-15-new-ideas.md`. The fidelity/QA fix queue is EMPTY.
- Narrow still-open true-edit item (not queued, low ROI): stroke-only text color (`Tr 1/2`) is untracked → Path-3 redraw fills instead of strokes.
- Commits this session: `73dd00d` S-FLOW · `386cd03` L2 · `db38ce1` ceiling-research · `8bb0244` P3 · `ae36591` DOCX#1 · `1a8a4c8` new-ideas.
- Dev server was running on :5173 (stale OCR langs: only 3/8 vendored — restart/`npm run ocr:assets --force` to realign; not a code bug).
- S-FLOW (P2) DONE + committed `73dd00d`. L2 (P2) DONE + committed `386cd03` (doc-only — /R 6 unreachable). Ceiling-challenge research committed `db38ce1` (PAdES/R6/lattice-tables are breakable; rest are real ceilings).
- P3 batch DONE (uncommitted, awaiting gate): **I1 = NOT-A-BUG** (live-verified with a real pointer gesture: image places at exact click coords, switches to select, no dupe — original QA was a Playwright synthetic-event artifact, 3rd false positive this session). **L1** fixed (owner-pw placeholder copy en/fr/ar + HTML fallback → "auto-generate a random one"). **A1** fixed (header lang-btn dark-glass bg → contrast 3.6:1→7.88:1, live-verified). **A2** fixed (mobile toolbar btn-icon/btn/btn-sm → 44px; 24/24 ≥44px live-verified). **D1** accepted, no change (toolbar density inherent; user-customizable toolbar; horizontal-scroll is the intended fallback — flex-wrap would shift the canvas). tsc/oxlint clean, 1046+2 tests.
- DOCX fidelity re-scoped: #3 number-tokenizer exponent ALREADY DONE (B-1, both tokenizers); Gap-4 nesting/markers ALREADY DONE. **#1 list continuation-merge DONE** (uncommitted, awaiting gate): `reconstructColumn` re-absorbs single-line, body-sized, hanging-indented marker-less continuation paragraphs into the prior list item so numbering stays contiguous; 3 new tests (TP merge + FP guard + e2e markdown numbering); 1049+2, tsc/oxlint clean.
- **NEXT: #2 Path-3 fill-color canvas-sample** (Separation/spot text redraws black; needs real-browser canvas sampling — `contentStreamEditor` Path-3 + `parseFillColorToRgb`). Then signing S8/S9.
- **Parallel ask (user): research genuine NEW ideas/tools/mechanisms to integrate — research+test before commitment.** Ceiling-breakers (PAdES via PKI.js, R6, lattice tables) in `docs/reviews/2026-06-15-ceiling-challenge.md`.
- Gate cadence: TDD, check in (AskUserQuestion) after each P1/P2 fix.

## S-FLOW design (ready to implement)
Problem (live-confirmed): in `src/core/pdfTurboApp.ts` `signPdf` (552-623), generate-mode generates the RSA cert + downloads `.p12`/`.pem` BEFORE any sign validation. On a placement/already-signed failure the user gets orphan cert downloads + no signed PDF; the `finally` (616-622) wipes `#signGenPassword` on EVERY exit → a naive retry silently bails at the `if(!genPw)` guard (565) while the stale error stays on screen. Default placement x=360/w=200 is off-page for pages <560pt wide (e.g. 500×400 fixture).
Signer validation order today (`src/signing/pdfSigner.ts` sign() 53-73): validateSignOptionsShape → `_assertNotAlreadySigned` → loadP12 → validatePageIndex → validateRect. All cert-independent EXCEPT loadP12.
Fix steps:
1. `pdfSigner.ts`: extract module fn `export function isPdfSigned(bytes: Uint8Array): boolean` (the regex from `_assertNotAlreadySigned`); refactor `_assertNotAlreadySigned` to throw based on it. Add `export` to signing/index.ts.
2. `pdfSigner.ts`: add `async preflight(pdfBytes, page, rect): Promise<void>` = `_assertNotAlreadySigned` + load doc + `validatePageIndex` + `validateRect` (the cert-free checks). Refactor `sign()` to call `preflight` (DRY) before `loadP12`.
3. `signingHandler.ts`: `sign(form, preassembled?: Uint8Array)` — use `preassembled ?? await app.assemblePdfBytes()` to avoid double-assembly.
4. `pdfTurboApp.signPdf`: assemble ONCE up front → `new PdfSigner().preflight(bytes, page0, rect)` in try/catch → on SignError show `sign.error.<code>` + return BEFORE cert-gen → then cert-gen/upload → `_signingHandler.sign(form, bytes)`.
5. Stop wiping `#signGenPassword` in the `finally`; keep the scrub in `closeSignModal` only (so a validation-failure retry keeps the typed password). Keep `signPassword` clear behavior consistent.
6. [P3 sub-item] Default placement: clamp the default rect to the target page OR shrink defaults so they fit small pages.
TDD: unit-test `isPdfSigned` (signed vs unsigned bytes) + `preflight` (throws INVALID_RECT for off-page rect, ALREADY_SIGNED for signed bytes, passes for valid) in `tests/signing/`; re-verify live that generate-mode with an off-page rect does NOT download a cert.

## Formal Plan
R2 done. S-FLOW next (design above). Then: L2 (R5→R6 check in src/export/encryption.ts; correct memory note if R6 unreachable), P3 batch (I1 add-image dup-stamp; L1 lock placeholder copy; A1 lang-btn contrast; A2 40px touch targets; D1 toolbar scroll), then queued DOCX fidelity (research-2026-06-15/), then signing S8 (/Contents right-size) + S9 (leaf-cert selection).

## Findings → fix queue (severity order)
1. **R2 [P1] Edit-Text broken on rotated pages** — ROOT CAUSE CONFIRMED (code): `src/handlers/textEditHandler.ts` `handleCanvasClick` maps the click to content coords with `pdfX = canvasX; pdfY = pageH - canvasY` (rotation-naive; only valid at 0°). At 90/180/270 the click never matches the unrotated-content-space text-item transforms → no hit → no editor. Fix: map via `viewport.convertToPdfPoint(canvasX*scale, canvasY*scale)` (the viewport already carries the rotation). Add jsdom/browser test rotating a fixture + asserting an editor opens / findTextOpAt hits.
2. **R1 [P2] textLayer not rotated with canvas** — CONFIRMED live (rot=270: canvas dims swap to 916×1145 but spans stay w=321/h=46 horizontal, identity matrix). `onPageStructureChange`→`renderCurrentPage`→`_renderTextLayer` DOES pass `effectiveRotation` (pageRenderPipeline:49), yet spans render unrotated. Mechanism still to pin: `_pageUpdatePending` early-return race, or pdf.js v6 TextLayer rotation detail. Impacts selection / Ctrl+F highlight / hover. Investigate then fix; browser test asserting span AABB rotates.
3. **S-FLOW [P2] signing generate-cert failure path** — `pdfTurboApp.signPdf` (552-623): cert generated + .p12/.pem downloaded BEFORE sign validation; `finally` wipes `#signGenPassword` on every exit → confusing retry + stale error; default placement off-page for <560pt pages. Fix: validate placement + already-signed BEFORE cert-gen; keep gen-password until success/close.
4. **L2 [P2] AES-256 lock /R 5 not /R 6** — `src/export/encryption.ts`. Verify if R6 reachable with @cantoo/pdf-lib; correct the memory/CLAUDE note claiming R6 if not.
5. **I1 [P3]** add-image dup-stamp · **L1 [P3]** stale lock placeholder · **A1 [P3]** lang-btn contrast · **A2 [P3]** 40px touch targets · **D1 [P3]** toolbar scroll @1440.

## Then (originally queued, post-QA)
- DOCX fidelity reachable items (list-marker→continuation merge, true-edit Path-3 fill-color canvas-sample, number-tokenizer exponent — see research-2026-06-15/).
- Signing S8 (right-size /Contents hex slot), S9 (leaf-cert selection from chain).

## Formal Plan
<!-- per-fix; R2 first (TDD) -->
