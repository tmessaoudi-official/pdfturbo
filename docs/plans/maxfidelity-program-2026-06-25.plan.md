# Max-Fidelity Program (true-edit + PDF→DOCX + DOCX editor) Plan

Design spec: `docs/superpowers/specs/2026-06-25-trueedit-docx-maxfidelity-program-design.md`

## Decisions Log
- [2026-06-25] AGREED: Improve true-edit + PDF→DOCX (+ DOCX editor) toward max reachable fidelity, no regressions.
- [2026-06-25] AGREED: Committed items DX-1 (struct-tree), DX-2 (lattice tables), TE-1 (Path-3 transform), TE-2 (Path-3 alpha).
- [2026-06-25] AGREED: Added TE-6 (XObject true-edit, spike-gated), DX-6 (outline→TOC), TE-7 (Path-3 bold/italic face), polish bundle (TE-3/5/8, DX-4) + DX-5 (headers/footers).
- [2026-06-25] AGREED: Added DOCX-editor items DXE-1 (display images), DXE-2 (image editing), DXE-3 (link survives save), DX-8 (Latin ligature NFKC). (Triggered by "do we support images in docx edit?" — verified: images preserved + PDF-export-rendered but NOT view-visible/editable.)
- [2026-06-25] AGREED: Structure = ONE umbrella program design doc → 2 feasibility spikes (TE-6, TE-1) → per-sub-project spec→plan→build, user approves each sub-project spec before its build.

- [2026-06-25] AGREED: After spikes (both positive), spec+build Sub-project B (PDF→DOCX) FIRST.
- [2026-06-25] AGREED: Sub-project B COMPLETE (5/5; B1 committed f0e2b72). NEXT = spec Sub-project A (true-edit) via the brainstorming flow → user review → plan → build.
- [2026-06-25] AGREED: A1 (Path-3 transform redraw) scope = FULL AFFINE — reproduce the entire trm (rotation + scale + shear) as the redraw Tm, using the BASE Tf size (not the vScale-baked fontSize) to avoid double-scaling. (Max fidelity over the lower-risk rotation+uniform-scale option.)
- [2026-06-25] AUDIT (code-is-truth): A4/TE-7 (Path-3 bold/italic face, matchStandardFont wired :2027) + A5/TE-4 (non-WinAnsi/ligature refuse, hasNonWinAnsi :2004) are ALREADY SHIPPED → dropped from Sub-project A. Remaining: A2 (alpha) · A3a (XObject Path-1/2) · A1 (full-affine transform) · A3b (XObject Path-3) · A6 (TE-5 dash/cap/join · TE-8 size-width · TE-3 rotated-input verify).
- [2026-06-25] AGREED: Sub-project A spec (`docs/superpowers/specs/2026-06-25-trueedit-subproject-a-design.md`) APPROVED → proceed to implementation plan. Build order A2→A3a→A1→A3b→A6.
- [2026-06-25] AGREED: Sub-project A COMPLETE (8 commits, gate green). Started Sub-project C brainstorming.
- [2026-06-25] VERIFIED (vite-node + jsdom probes, real docModel.ts): the DOCX editor `save()` is currently DATA-LOSSY — an image-bearing top-level paragraph parses to `{runs:[]}` and `setRunsOn` wipes the `w:drawing` (DRAWING_SURVIVES:false); a `w:hyperlink` survives but `parseParagraph`'s deep run read makes save APPEND a duplicate plain run (text appears twice). The prior memory claim "images preserved through save" was WRONG (it was the read-only export `getImages()` channel, not the save round-trip).
- [2026-06-25] AGREED: Sub-project C = "Fix-first, then enrich". Phase 1 = preservation (image verbatim passthrough + hyperlink de-dup, pure correctness, own gate + byte-identical-when-absent controls). Phase 2 = display images (C1) + edit images (C2) + link editing (C3). Each phase boundary is a shippable gated state.
- [2026-06-25] AGREED (Claude recommended, user "you decide"): Image-preservation seam = whole-paragraph OPAQUE ANCHOR. New `DocImageBlock {kind:'image'}` as a third opaque DocBlock variant; reconciler skips it (verbatim passthrough) like tables. Detection = paragraph contains any `w:drawing` (inline or floating → both preserved). Ceiling: a paragraph mixing flowing text + inline image is read-only in Phase 1 (documented). Clean anchor for Phase 2 display/edit.
- [2026-06-25] AGREED: Phase-1 hyperlink de-dup = UNIFORM opaque anchor. One rule: a top-level `w:p` containing a `w:drawing` OR a `w:hyperlink` → opaque passthrough anchor (reconciler skips it; never duplicates/reorders; link & drawing preserved byte-exact). That paragraph is read-only until Phase 2 C3 adds the proper `w:hyperlink`↔link-mark+rels round-trip. Avoids the descend-skip reordering risk.
- [2026-06-25] AGREED: Phase-1 editor representation = render the REAL image inline (read-only PM atom showing the extracted PNG/JPEG) + hyperlink text read-only. Folds C1 "display images" INTO Phase 1 (read-side only, save-isolation intact). So Phase 1 = preserve+display, Phase 2 = edit (C2 move/resize/delete images, C3 editable links). In Phase 1 anchors are non-deletable (deletion is a Phase-2 C2 feature; a deleted atom simply persists on save — safe, never data-loss).
- [2026-06-25] DESIGN INVARIANT (Claude, robustness): anchor detection is DOM-STRUCTURAL at reconcile time — a `w:p` containing `w:drawing|w:hyperlink` is hard-SKIPPED by the reconciler in BOTH the main and the table-count-mismatch fallback path, independent of model/PM bookkeeping. (The existing table fallback `reconcileParagraphsOnly` filters `w:p` and would feed an anchor to `setRunsOn` → must exclude anchors there too.) Guarantees preservation can't break even if the PM doc diverges from the DOM (e.g. user deletes the atom).
- [2026-06-26] AGREED: Sub-project C Phase 1 (preserve+display) DONE `21aab3f`; Phase 2a (editable external hyperlinks, C3) DONE `17a4999`..`084492b`. All pushed (origin `084492b`). NEXT = Phase 2b (C2 image edit).
- [2026-06-26] AGREED: C2 v1 scope = DELETE + RESIZE only. Move/reorder + new-image insert → v2 (insert is the documented stretch). Lowest-risk highest-value slice; reorder needs cross-position identity, insert needs new media part+rels+Content_Types.
- [2026-06-26] AGREED: C2 resize/delete UX = drag handles (corner, aspect-locked, Shift=free) + Delete key / inline ✕, via a `docx_image` ProseMirror NodeView (real-Chrome pixel-guarded; jsdom can't lay out drags).
- [2026-06-26] DESIGN (Claude): C2 identity = `anchorId` stamped at parse = 0-based index among TOP-LEVEL drawing anchors (w:p with w:drawing), carried on the docx_image node, read back on save. A save PRE-PASS `reconcileImageAnchors` (in applyBlocks, before reconcileContainer) deletes the w:p where the anchorId is absent and rewrites wp:extent (+inner a:ext when present) cx/cy ONLY when dims differ (byte-exact when no image edited). SAFETY GUARD: if surviving anchorIds aren't a duplicate-free subset of {0..m-1} → skip the pre-pass entirely (Phase-1 verbatim fallback, never corrupt). Hyperlink anchors (docx_link, no anchorId) + tables + cell-nested images stay fully opaque.
- [2026-06-26] AGREED (Claude revised rec after user challenge): FOLD editor-wide undo into C2 — add `prosemirror-history` + Mod-z/Mod-y (new dep, MIT, currently absent). Rationale: C2 adds the editor's first DESTRUCTIVE ops (delete, mis-drag resize), otherwise unrecoverable; the moment to add the safety net. Must compose with findReplacePlugin's single-step replace-all batching + tableEditing transactions (tested).
- [2026-06-26] 3C gate (full 30/8) surfaced + fixed 6 real issues before any code: mixed-para whole-w:p delete semantics, PDF-export staleness ceiling, resize px↔pt base (vs max-width clamp), the `applyParagraphRuns` mass-delete regression (→ `editImages` opt-in), the unsupported-format-image delete regression (→ anchorId on docx_link fallback + identity-only S), and lint (`!`/`as any`). Converged 8/8 at cycle 30.
- [2026-06-26] DONE: **Sub-project C Phase 2b COMPLETE → the whole max-fidelity program (A+B+C) is COMPLETE.** Commits: `66e2789` (spec+plan) · `7431e45` (T1 anchorId) · `5e463c3` (T2 pre-pass) · `bfbb0f9` (T3 NodeView UI) · `eb17815` (T4 undo) · `3adbad0` (T5 docs). FULL deploy gate GREEN: audit 0 · ocr · type-check · oxlint · jsdom 2217+2 · browser 168 · coverage:export 44.07% · build. Eyes-on: qa-shots/c-phase2b/resize.png. UNPUSHED (push MANUAL).
- [2026-06-26] AGREED (post-program follow-ups, ordered A→C→B→D; user "all of them"): see "## Next session" below. Claude challenged D (full sweep) as low-marginal-value right after a green gate + flagged prosemirror-history as the real blast-radius risk; user kept D as a final gate.
- [2026-06-26] DONE: Follow-up A (live eyes-on QA of C2 + undo composition) PASSED clean. Real .docx + real 240×160 PNG: resize aspect-locked (240×160→330×220), Shift=free (→330×270), ✕/Delete removes, Ctrl+Z/Y undo/redo resize AND delete; composition verified live — typing-undo (image survives), find/replace replace-all reverts in ONE step, table add-row undo (2→3→2). Max-width clamp clear (2 sequential drags = exact arithmetic, base reads stored widthPt). 0 console errors. Evidence qa-shots/c-phase2b-live/.
- [2026-06-26] AGREED: Follow-up B = ALL move triggers + insert (user "all of them"). Build ORDER (user-confirmed, Claude-recommended, foundation-first/risk-ascending): (1) INSERT (toolbar→file picker) · (2) MOVE-core = save-side DOM-anchor reorder driven by ▲/▼ + Alt+↑/↓ · (3) Cut&paste (PM clipboard atom round-trip) · (4) Drag-to-reorder (overlaps the resize-handle drag → hit-test image-body=move vs SE-handle=resize; last + riskiest). Each = own spec/plan/build/gate; all 4 triggers share ONE save-side reorder built in (2).
- [2026-06-26] AGREED: Follow-up B scope = BOTH new-image INSERT + image MOVE/reorder (user chose "both" over Claude's recommended insert-only). DECOMPOSE into two sub-slices, build INSERT first (lower risk: ensureImagePart + add-anchor pass; reuses opcParts register-if-missing), then MOVE (reworks reconcileContainer's boundary-zip-in-order invariant to relocate DOM anchor w:p by stable id — the corruption-risk part). Each sub-slice = own spec/plan/build/gate, lands working independently.
- [2026-06-26] DONE: Follow-up C COMPLETE. `docxToPdf` renders images from the live model's `DocImageBlock.image` (resize/delete reflected); stale `getImages()`/`opts.images`/`imagesByBlock` channel REMOVED; controller calls `docModelToPdfBytes(model)`. Commits `50f78ca` (spec+plan) · `539f308` (fix+tests). FULL deploy gate GREEN: audit 0 · ocr · type-check · lint · jsdom 2218+2 · browser 170 · coverage 44.07% · build. Live eyes-on `qa-shots/c-export-resized.pdf` (resized image baked in). UNPUSHED (push MANUAL). NEXT = follow-up B (image move/insert), then D (whole-app sweep).
- [2026-06-26] AGREED (user): after B slice 2, DESIGN B sub-slice 3 (Cut & paste) via brainstorming → spec → review → plan → build (per-item). Reuses the slice-2 `placeImageAnchors` insert path on paste.
- [2026-06-26] AGREED (B slice 3 scope, Option 1): intra-editor cut/copy/paste of an existing image (Ctrl+X/C/V) AND paste an EXTERNAL image blob (OS clipboard / screenshot → new docx_image minted on save, reusing slice-1 bytes→node logic). EXCLUDES http-URL/web-HTML images (CORS/fetch). CORE MECHANISM (verified): docx_image has toDOM but NO parseDOM, and PM default copy/paste preserves attrs → a copy duplicates anchorId → trips the save dup-free safety guard. Fix = a `transformPasted(slice)` that RESETS anchorId→-1 on any pasted docx_image (paste = NEW image, minted fresh; cut→paste = delete-original + insert-new, re-mints media, orphaning the old part exactly like C2 delete already does) + a `parseDOM` rule on docx_image (img[data-docx-image], data-uri src → dataB64/mime, anchorId -1) for cross-session/HTML robustness; external blob via handlePaste reading clipboard image items. existing transformPastedHTML/handlePaste don't conflict (transformPasted is a separate prop). Brainstorm Q1 ANSWERED; design not yet presented.
- [2026-06-26] DONE: B sub-slice 3 (image CUT & PASTE) COMPLETE. Option 1 (intra-editor cut/copy/paste + external image-blob; NO http/web-HTML CORS). 5-task TDD per-item: `801442a`(spec+plan) · T1 `resetPastedImageAnchors` · T2 scoped `parseDOM` · T3 blob helpers + DRY toolbar lift · T4 wire transformPasted+handlePaste · T5 browser+docs. THREE units, NO new save logic (reuses slice-1/2 mint/insert), new `src/docx/docxImagePaste.ts`: (1) `resetPastedImageAnchors` as `transformPasted` resets pasted docx_image anchorId→-1 (covers slice + HTML-parse paths → copy/paste AND cut/paste; the fix for the dup-anchorId verbatim-bail that silently dropped the pasted copy); (2) `parseDOM` `img[data-docx-image]`+`data:png|jpeg` `priority:60` (root-cause: wins over schema-basic's inline `image` `img[src]`); (3) `handlePaste` image-blob (`firstImageFile`→`insertImageBlob`). CUT=PM-native copy+delete (old part orphaned like C2). LIFTED sniff/b64/dims+PT consts toolbar→docxImagePaste (DRY, byte-identical). Ceiling: http(s) src, GIF/SVG/WebP, orphaned-media GC, mixed text+image HTML. FULL gate GREEN (audit0/ocr/tsc/oxlint/jsdom 2250+2/coverage 44.07%/build; browser 172 pass +1 KNOWN-FLAKE issue2-true-edit which passes 1/1 isolated — a true-edit test untouched by this slice). Eyes-on qa-shots/b-cutpaste/{before-one-image,after-two-images}.png (copy→paste duplicates inline). CLAUDE.md gotcha + memory updated. UNPUSHED (push MANUAL). NEXT = B sub-slice 4 (Drag-to-reorder), then D.
- [2026-06-26] AGREED (B slice 3 design APPROVED, "Approve → spec + plan"): three small units, all reusing the slice-1/2 mint/insert engine (no new save logic) — (1) `transformPasted(slice)` PM prop walks the pasted fragment and rebuilds every `docx_image` with `anchorId:-1` (the universal hook covering intra-editor copy/paste AND cut/paste; PM runs it on the final slice for both the internal-slice and HTML-parse paths); (2) a scoped `parseDOM` on `docx_image` matching `img[data-docx-image]` with a `data:` src only → `{dataB64,mime,anchorId:-1}` (cross-instance HTML-clipboard robustness; arbitrary web `<img>` never matches); (3) a `handlePaste` image-blob branch (after the existing plain-paste check) reading an `image/png|jpeg` clipboard blob → `createImageBitmap` dims (slice-1 logic) → insert `docx_image` `anchorId:-1`. Cut = native copy+delete (original w:drawing removed by reconcileImageAnchors, paste re-mints → move-via-clipboard, old part orphaned like C2 delete). Ceiling: http(s) `<img src>` (CORS), GIF/SVG/WebP, orphaned-media GC, mixed text+image HTML. Spec next.
- [2026-06-26] DONE: B sub-slice 2 (image MOVE/reorder) COMPLETE. T1 anchorEl identity map + map-keyed reconcileImageAnchors `1f5c8c3` · T2 placeImageAnchors (move existing by anchorId + insert new, absorbs materializeNewImageAnchors) `d552db7` · T3 moveImage command + NodeView ▲/▼ + Alt+↑/↓ keymap `065c0de` · T4 browser e2e + gate + docs. Full fidelity (displaced paragraph pPr survives — asserted); byte-identical when nothing moved; reconcileContainer untouched. Gate: audit 0/ocr/tc/lint/jsdom 2240+2/coverage 44.07%/build 489 modules GREEN. Browser: docx-image-move 2/2 green; the FULL browser suite showed PRE-EXISTING non-deterministic canvas/pixel flakiness (run1: redaction×2+trueedit-sequential; run2: issue2-true-edit — different set each run, ALL pass in isolation → contention flake, NOT a regression; my changes touch only src/docx/* + locales). Live eyes-on qa-shots/b-move/move-controls.png (▲/▼ on selected image). UNPUSHED (push MANUAL). NEXT = B sub-slice 3 (Cut&paste).
- [2026-06-26] AGREED (B slice 2 design APPROVED): two halves — PM-side `moveImage(dir)` command (reorders the docx_image node, undoable; NodeView ▲/▼ + Alt+↑/↓) + save-side `placeImageAnchors` (unified walk) keyed off a once-built `anchorEl: Map<anchorId,Element>` shared with a map-keyed-refactored `reconcileImageAnchors` (removes the positional footgun). reconcileContainer untouched; full fidelity (no paragraph rebuild). applyBlocks always replays from pristine originalXml → multi-move composes, no mid-session anchorId churn. Spec next.
- [2026-06-26] AGREED (B slice 2 engine, Claude recommended): save-side reorder = UNIFIED `placeImageAnchors` walk generalizing slice-1's materializeNewImageAnchors (existing image by anchorId → MOVE to cursor; new → INSERT; in-place → no-op; cursor skips image elements, text+tables are fixed reference points). Full fidelity (no paragraph rebuild), reconcileContainer untouched. Rejected boundary-reorder+reconcile (rebuilds displaced paragraphs, pPr loss) and delete+re-insert (re-mints media, file bloat).
- [2026-06-26] AGREED (B slice 2 UI, Claude recommended): move controls = ▲/▼ buttons ON the selected image's NodeView (beside C2's ✕/resize handles) + Alt+↑/↓ keyboard when an image is selected; each press moves the image past ONE adjacent top-level block.
- [2026-06-26] AGREED (B slice 2 scope, Claude recommended): MOVE = IMAGE-only, ANY distance (past text AND crossing tables/other images). Tables/paragraphs themselves stay put; full arbitrary-block reorder is out of scope (would need stable identity for every boundary + text). Images have anchorId identity → tractable + bounded risk.
- [2026-06-26] AGREED (Claude recommended, user "what do you recommend, reask"): after B slice 1, DESIGN B sub-slice 2 (MOVE-core) via the brainstorming skill BEFORE building — it's the corruption-risk slice (reworks reconcileContainer's boundary-zip-in-order to relocate a DOM anchor w:p by stable id; slices 3/4 inherit its reorder primitive). Brainstorm → spec → user review → plan → build gated per-item. NOT the straight-through 2→4 run (skips design checkpoint on the risky piece).
- [2026-06-26] DONE: Follow-up B sub-slice 1 (new-image INSERT) COMPLETE. 4 tasks TDD: T1 `opcParts.ensureImagePart` (mint word/media part + Content-Types Default + image rel) `22a14ab` · T2 `docModel.buildDrawingParagraph` + `materializeNewImageAnchors` (parallel model/DOM walk) + `applyBlocks` `opts.mintImage` callback `2cffcf8` · T3 editor save wiring + toolbar 📷 Insert button + file-pick (sniff+createImageBitmap dims) + browser test `c5fed77` · T4 gate+shot+docs. **Ordering deviation from spec (data-loss fix):** reconcileImageAnchors runs FIRST (keys on parse-time anchor POSITIONS — inserting first would shift them → wrong delete/resize), THEN materialize, THEN reconcileContainer; proven by the insert-BEFORE-existing test. Byte-identical when no image inserted. FULL deploy gate GREEN (audit 0 / ocr / tc / lint / jsdom 2227+2 / browser 171 / coverage 44.07% / build 488 modules). Live eyes-on `qa-shots/b-insert/inserted.png` (blue image renders inline + ✕ handle). UNPUSHED (push MANUAL). NEXT = B sub-slice 2 (MOVE-core).
- [2026-06-26] AGREED (Approach 1, Claude recommended): Follow-up C = DOCX→PDF export-staleness fix via SELF-RENDERING image blocks. Root cause: `docxToPdf.docModelToPdfBytes` renders images from the stale `opts.images`/`getImages()` channel (original dims/positions, deleted images still present), keyed positionally by `blockIndex`; the LIVE model already carries `DocImageBlock.image` {dataB64,mime,widthPt,heightPt} round-tripped through the PM node (resize reflected) and a deleted image's block is absent. Fix: render each DocImageBlock from its OWN `.image` in the `model.blocks` loop; DROP the `imagesByBlock`/`opts.images` indirection; controller stops passing `getImages()`. Net code deletion, one source of truth. Rejected Approach 2 (reconcile by anchorId — redundant/fragile) + Approach 3 (re-extract from save() — heavy round-trip).

## Next session — RESUME: build phase-B sub-slice 1 (Insert)

> Progress through A→C→B→D: **A DONE**, **C DONE**, **B slices 1 (INSERT) + 2 (MOVE) + 3 (CUT&PASTE) DONE**. RESUME = **B sub-slice 4 (Drag-to-reorder)** — the last + riskiest: the drag OVERLAPS the C2 SE resize-handle drag, so hit-test the image BODY = move (reuse `placeImageAnchors`/`moveImage`) vs the SE-handle = resize. Brainstorm → spec → plan → build per-item. Then **D = whole-app /qa-sweep**. Verify `git log origin/master..HEAD` first (unpushed commits accumulate; push is MANUAL). NOTE: the full browser suite has pre-existing non-deterministic canvas/pixel-test flakiness under contention — re-run flaky failures in isolation to confirm before treating as a regression.

- [x] **A — Live eyes-on QA of C2 + undo composition.** DONE 2026-06-26. Real .docx + real 240×160 PNG: resize aspect-locked (240×160→330×220), Shift=free (→330×270), ✕/Delete, Ctrl+Z/Y undo/redo resize+delete; composition LIVE — typing-undo (image survives), find/replace replace-all ONE undo step, table add-row undo (2→3→2); max-width clamp clear; 0 console errors. Evidence `qa-shots/c-phase2b-live/`. prosemirror-history blast-radius disproven.
- [x] **C — DOCX→PDF export staleness fix.** DONE 2026-06-26 (Approach 1, self-rendering blocks). `docxToPdf` renders images from the live model's `DocImageBlock.image`; stale `getImages()`/`opts.images`/`imagesByBlock` REMOVED; controller calls `docModelToPdfBytes(model)`. Gate GREEN (jsdom 2218+2/browser 170/coverage 44.07%/build). Live eyes-on `qa-shots/c-export-resized.pdf`. Commits `50f78ca`(spec+plan)·`539f308`(fix)·`e26945d`(docs).
- [ ] **B — image MOVE + new-image INSERT (user "all of them"; decomposed into 4 sub-slices).** Build ORDER = (1) INSERT · (2) MOVE-core (▲/▼+Alt save-side reorder) · (3) Cut&paste · (4) Drag. Each own spec/plan/build/gate; all 4 triggers share ONE save-side reorder built in (2).
  - [x] **B sub-slice 1 — INSERT. DONE 2026-06-26.** `22a14ab` (ensureImagePart) · `2cffcf8` (buildDrawingParagraph + materializeNewImageAnchors + applyBlocks opts.mintImage callback) · `c5fed77` (save wiring + 📷 toolbar + file-pick + browser test) + docs. Ordering: reconcileImageAnchors → materialize → reconcileContainer (data-loss fix vs spec). Gate GREEN; eyes-on `qa-shots/b-insert/inserted.png`.
  - [x] **B sub-slice 2 — MOVE-core. DONE 2026-06-26.** `1f5c8c3` (anchorEl map + map-keyed reconcileImageAnchors) · `d552db7` (placeImageAnchors move+insert) · `065c0de` (moveImage command + NodeView ▲/▼ + Alt+↑/↓) + docs. Full fidelity, reconcileContainer untouched, byte-identical when unchanged. Spec/plan `…2026-06-26-docx-image-move*`. Gate GREEN; eyes-on `qa-shots/b-move/move-controls.png`.
  - [ ] **B sub-slice 3 — Cut & paste (PM clipboard atom round-trip → reuses slice-2 `placeImageAnchors`).** Spec→plan→build. NEXT ACTION = brainstorm/spec slice 3.
  - [ ] B sub-slice 4 — Drag-to-reorder (hit-test: image body=move vs SE handle=resize; riskiest, last). Spec→plan→build.
- [ ] **D — Whole-app /qa-sweep** (final gate, user kept it). After all of B. `/qa-sweep` browser target at the dev server, full feature crawl + convergence + before/after screenshots.

## Program structure (locked)
- Sub-project A — True-edit: A1 TE-1 · A2 TE-2 · A3 TE-6(spike) · A4 TE-7 · A5 TE-4 · A6 polish(TE-3/5/8)
- Sub-project B — PDF→DOCX: B1 DX-1 · B2 DX-2 · B3 DX-6 · B4 DX-3 · B5 DX-5 · B6 DX-4 · B7 DX-8
- Sub-project C — DOCX editor: C1 DXE-1 · C2 DXE-2 · C3 DXE-3
- Order: spikes → B → A → C

## Hard walls (out of scope)
In-place Arabic/RTL, Type3, vector→OOXML, exact subset face, borderless tables, multi-line reflow,
text-clip modes 4–6, TSA/CA (network).

## Formal Plan
<!-- per-sub-project plans written by writing-plans at each sub-project's Phase 4 -->
- [x] Spike-1 TE-6 XObject true-edit — POSITIVE (engine write-back exists; Path-1/2 complete; handler gate is the only block). TE-6 SPLITS: TE-6a (Path-1/2, low risk, early) + TE-6b (Path-3, rides A1).
- [x] Spike-2 TE-1 Path-3 transform capture — POSITIVE (`trm` computed at `:531`, discarded in `buildPath3Redraw :2103`). Must decompose rotation+scale vs the vScale-baked fontSize. A1 sequences before TE-6b.
- AUDIT 2026-06-25: B2 (lattice tables) + B4 (heading bold/caps promotion) are ALREADY SHIPPED (verified in code: detectLatticeTables/FlowTable/mkTable + assignHeadings G11) → removed from scope. Scorecards were stale.
- [x] Sub-project B spec → `docs/superpowers/specs/2026-06-25-docx-export-subproject-b-design.md` (5 open items: B7 ligature, B3 outline→TOC, B6 3-col recursion, B5 headers/footers, B1 struct-tree exact-replace). Build order B7→B3→B6→B5→B1; B1 behind a marked-content correlation sub-spike.
- [ ] Sub-project B build (per the spec, item-by-item, each own commit + full gate + byte-identical-when-inactive guard)
  - [x] B7 Latin ligature fold — `foldLatinLigatures` (flowDoc.ts) + wired at reconstructPage word-build; tests/utils/flowDocLigature.test.ts (5, incl. wire guard + byte-identical control); type-check/lint/208-flowDoc-regression green. STAGED, not committed.
  - [x] B3 outline→Word TOC field — `flattenOutline` + `FlowDoc.outline` (flowDoc.ts) + TOC field in flowDocWriters.ts (gated outline+heading) + getOutline fetch in exportService._extractFlowDoc; tests/utils/flowDocOutline.test.ts (6, incl. 2 no-TOC controls); type-check/lint/215-regression green. STAGED.
  - [x] B6 3-col recursive XY-cut — `splitColumns` (recursive, depth-2) + `detectColumnSplit` optional `bounds` arg (default→byte-identical); wired in reconstructPage. tests/utils/flowDocColumns.test.ts (5, incl. 1/2-col byte-identical). 220-regression green. STAGED.
  - [x] B5 headers/footers — `detectRepeatedBands` + `applyRepeatedBands` (conservative ≥3pg/≥60%/tight-band/digit-norm; removes hoisted inline dup) + `FlowDoc.header/footer` + Header/Footer per section in writer; wired in exportService. tests/utils/flowDocHeaderFooter.test.ts (8, incl. no-false-positive + no-op control + writer part). 227-regression green. STAGED.
  - [x] B1 SUB-SPIKE done (Node, legacy pdfjs, w3c-accessible-table.pdf): roles Root/Sect/H1/P/Table/TR/TH/TD; **100% correlation** — all 38 struct content-leaf ids match `getTextContent({includeMarkedContent:true})` marker ids (`beginMarkedContentProps {id,tag}`). → **exact-replace VIABLE, no HINTS fallback needed.**
  - [x] B1 BUILD DONE: `buildMarkedContentMap` (innermost-MCID attribution, no-MCID artifacts dropped) + `structTreeToFlow` (walks H1-6→heading / P/Note/Caption/Quote→body / L+LI→list w/ depth+ordered-marker detect / Table+TR+TH/TD→FlowTable; Figure skipped; returns null→heuristic fallback when zero text resolved) + `buildRunsFromLines` extracted from buildParagraph (reused, byte-identical) + `FlowPage.tagged` + assignHeadings skips tagged pages (all 3 loops) + reconstructPage `struct?` param (exact-replace, margins still from words) + exportService fetches getStructTree()/includeMarkedContent and passes `struct` (untagged → plain getTextContent, byte-identical). tests/utils/flowDocStructTree.test.ts (10) + tests/browser/docx-structtree.browser.test.ts (2: real tagged PDF heading+table; untagged byte-identical). FULL deploy gate GREEN (audit 0 / tc / lint / jsdom 2168+2 / browser 157 / coverage:export 44% / build). **Sub-project B COMPLETE (5/5).** Ceiling: partially-tagged page drops untagged-as-artifact text (exact-replace contract); alignment/indent not tag-derived; multi-column tagged page reading-order rides y-sort.
- [x] Sub-project A spec → plan → build — COMPLETE 2026-06-25. Spec `docs/superpowers/specs/2026-06-25-trueedit-subproject-a-design.md`, plan `docs/superpowers/plans/2026-06-25-trueedit-subproject-a.md`. Built A2 (alpha `14f5a55`) → A3a (XObject Path-1/2 `a5bc8f3`) → A1 (full-affine transform `6586c23`) → A3b (XObject Path-3 `5d0cb2e`) → A6 (dash/cap/join + size-change deco width + rotated-input guard `3b9a553`); A2-fixup `7ba455a`. AUDIT dropped A4 (matchStandardFont already wired) + A5 (hasNonWinAnsi already refuses) as ALREADY SHIPPED. Each item own commit + full gate (final: jsdom 2186+2, browser 161). All gated/additive → byte-identical at defaults. KEY traps handled: A3a XObject-aware font introspection (else Path-1 corrupts XObject CID fonts); A1 base-Tf-size to avoid double-scaling; A3b XObject-local coords (no fallback needed).
- [~] Sub-project C — IN PROGRESS. Spec `docs/superpowers/specs/2026-06-25-docx-editor-subproject-c-design.md` (APPROVED). Phase-1 plan `docs/superpowers/plans/2026-06-26-docx-editor-subproject-c-phase1.md` (6 TDD tasks: DocImageBlock → anchor parse → reconciler hard-skip [the P0 fix] → atom nodes → PM bridge+byte-merge → real-Chrome e2e). Build = "fix-first, then enrich": Phase 1 = preserve images/hyperlinks + display images read-only; Phase 2 (later) = C2 image edit + C3 editable links.
  - [x] Phase 1 build DONE 2026-06-26: `DocImageBlock` opaque variant + DOM-structural anchor detection (`isAnchorParagraphEl`) + reconciler hard-skip (tables AND drawing/hyperlink paragraphs are immutable boundaries, skipped in main + fallback paths) → images & hyperlinks survive save byte-exact, no duplication; `docx_image`/`docx_link` read-only atom nodes render the real image / link text; `mountDocxEditor` merges `extractDocImages` bytes by block index; `docxToPdf` skips image blocks (drawn via the existing images channel). Tests: `tests/docx/{docModelImagePreserve,docxImageBridge}.test.ts` (jsdom) + `tests/browser/docx-image-preserve.browser.test.ts` (real Chrome: img renders, link once, save round-trips drawing+blip+single hyperlink). FULL deploy gate GREEN (audit 0 / type-check / lint / jsdom 2195+2 / browser 162 / coverage:export 44% / build). Ceiling: a paragraph mixing text+inline-image/link is read-only; anchors non-deletable/non-reorderable in Phase 1.
  - [ ] Phase 2a = C3 editable links FIRST, then Phase 2b = C2 image edit (AGREED 2026-06-26).
  - [2026-06-26] AGREED (C3 scope): EXTERNAL hyperlinks (`r:id`→http/https/mailto) become fully editable via a per-run `DocRun.linkUrl?` ↔ the existing PM `link` mark (href) + a `document.xml.rels` round-trip (reuse-or-create relationship; `sanitizeLinkUrl` gate on save). This SUPERSEDES Phase-1's hyperlink-opaque rule (de-dup becomes structural: parse reads hyperlink runs once with linkUrl, save re-emits one `w:hyperlink`). INTERNAL-anchor links (`w:hyperlink` with `w:anchor` and NO `r:id`) stay OPAQUE/preserved (read-only) — `isAnchorParagraphEl` becomes: opaque if `w:drawing` OR a `w:hyperlink` that is internal-anchor-only. A paragraph mixing an external + an internal-anchor link stays opaque (preserve the internal one). Internal-link editing = documented ceiling.
  - [x] Phase 2a (C3 editable external hyperlinks) DONE 2026-06-26: `DocRun.linkUrl`↔`link` mark; `isAnchorParagraphEl` opaque only for `w:drawing`|internal-anchor hyperlink; `parseParagraph` child-walk reads hyperlink runs once (linkMap rId→URL); save groups same-linkUrl runs into one `w:hyperlink` (rId via `opcParts.ensureHyperlinkRel`, `sanitizeLinkUrl`-gated); toolbar 🔗 button+inline input. Byte-identical when no link. Internal-anchor links stay opaque/preserved (ceiling). Commits 17a4999/dad8f62/2d40576/c1c958a + e2e. FULL gate GREEN (jsdom 2205+2, browser 164, coverage 44%, build). Spec/plan `…2026-06-26-docx-editor-subproject-c-phase2a-links*`.
  - [ ] Phase 2b (C2 image move/resize/delete) spec→plan→build
