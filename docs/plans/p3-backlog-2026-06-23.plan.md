# P3 backlog (QA sweep 2026-06-23) — fix plan

## Decisions Log
- [2026-06-23] AGREED: After Options 2/3/4 (all P1+P2) shipped + PUSHED by the user,
  do **Option 2 (this P3 backlog) THEN Option 3 (feature backlog / ceilings)**.
- [2026-06-23] AGREED: clear the 28 P3 findings from `docs/reviews/2026-06-23-qa-sweep.md`
  in themed batches (like the P2s), risk-ascending, TDD, one commit per theme.
- [2026-06-23] User PUSHED the 13 prior commits (`a6c9ab2..5998749`) to origin/master.

## Context
- Baseline: 0 P0 · 4/4 P1 · 34/34 P2 done. 28 P3 open. Gate green: jsdom 1947+2/169 + browser 132/52.
- Workflow: `node_modules/.bin/vitest run --reporter=dot > /tmp/.../log 2>&1` then grep (RTK proxy mangles npm test output). Browser: `--config vitest.browser.config.ts`. NO Co-Authored-By. Push is MANUAL.
- Locale changes must hit en/fr/ar (hook enforces); Arabic = [Unverified].

## Recommended order (risk-ascending, value-first)

### Theme A — Security & input hygiene (5) — FIRST (independent, high-value, low blast radius)
- **#14** PDF Link-annotation URL set on `a.href` with no in-app scheme check (relies on pdf.js). Add a scheme allowlist (http/https/mailto) — reuse `safeMdUrl`-style guard. (textLayer link wiring.)
- **#15** `cleanWordHtml` keeps pasted `<a href>` without sanitizing the URL scheme. Allowlist schemes (drop javascript:/data:). `src/docx/wordPaste.ts`.
- **#16** CSP allows `style-src 'unsafe-inline'`. Investigate removing it (may need nonce/hash for injected styles) — assess feasibility; document if structurally required.
- **#17** PDF text-search regex path has no ReDoS guard (overlay path it shares with does). Add the same cap/guard. `textSearchHandler` / find path.
- **#18** Stale doc comment: claims `escapeValue:false` but code is `escapeValue:true` (code correct) — fix the comment. Trivial.

### Theme B — Correctness bugs (4)
- **#4** Custom blank-page dims accept NaN / non-positive silently. NaN-safe `intOr`/clamp (mirror the Bates `intOr` pattern). Blank-page modal.
- **#5** Title-case lowercases token TAILS, losing intentional internal caps (e.g. "iPhone"). `src/utils/textCase.ts` — only uppercase first char, don't lowercase the rest. (Has tests — update.)
- **#6** `replaceShowOpHex` TJ slice can split a 2-byte code when an original hex segment had odd length. Distribute on even byte boundaries. `contentStreamEditor.ts`. (Engine — careful + guard.)
- **#7** XFDF import doesn't normalize inverted/negative-size rects (`w=x2-x1` w/o Math.min). Normalize. `src/utils/xfdf.ts` / `xfdfMapping.ts`.

### Theme C — a11y (3)
- **#1** Deleting a page via keyboard leaves focus on a removed DOM node. Move focus to a sibling/adjacent thumbnail after delete.
- **#2** Restore dialog: `role=dialog aria-modal` but no focus trap / Esc. Add `trapFocus` + Esc (pattern already used by other modals).
- **#3** Resize/rotate handles pointer-only; delete button lacks aria-label. Add aria-labels / keyboard affordance where feasible.

### Theme D — UX feedback & polish (9)
- **#26** Text-background color is a LONE picker, not the shared palette — reuse the presets+recent+swatch system (ties to the [[feedback_reuse_color_palette]] standing rule). `ui.textBgColor`.
- **#22** No toast confirming a redaction was placed (only the in-box label). Add a placement toast.
- **#23** Redaction warning style keys on exact `'#000000'` string — normalize/compare robustly.
- **#28** CSV download lacks UTF-8 BOM → Excel mojibake. Prepend BOM. `exportService.exportTableCsv`.
- **#11** `renderComment` truncates exported comment text to 200 chars silently — warn or raise the cap.
- **#19** `editedName` mangles non-.docx filenames (`foo.txt` → `foo.txt-edited.docx`). Strip the real ext. `docxEditorController`.
- **#20** Element commands silently no-op when target id missing (no feedback). Add a guard/log.
- **#24** Signature appearance text band can shrink to ~0 when an appearanceImage is embedded → silently drops signer/date. Clamp min band.
- **#25** `dataUrlToBytes` silently drops a non-PNG drawn signature (depends on pad default PNG). Handle/guard.

### Theme E — Data-safety footguns & honest docs (7) — LAST (mostly document-the-design)
- **#9** `setPageContent & 0xff` mask silently truncates chars ≥256 (lossless today). Guard/assert.
- **#10** Lossy-compress hint understates loss (drops links + form fields, not just text). Fix the wording (3 locales).
- **#12** XFDF y-flip ignores per-page crop (rotation already a documented ceiling). Document or fix.
- **#27** `addDecorationAt` embeds a proxy font purely to measure width → orphan font dict. Measure without embedding, or reuse.
- **#21** `exportOcrText` clipboard copy can fall outside transient activation (already handled — file still downloads). Verify, likely no-op/doc.
- **#8 / #13** Form values + original unredacted source bytes persisted to IndexedDB in CLEARTEXT. This is consistent with the no-backend design (everything is local). Right action = DOCUMENT the tradeoff honestly (KNOWN_ISSUES / privacy note), NOT silent at-rest encryption (that's a feature, not a P3). Confirm scope with user before any crypto work.

## Gate per theme
type-check + oxlint + jsdom + relevant real-Chrome browser tests; one commit per theme; mark report P3s fixed.

## After P3 → Option 3 (feature backlog / ceilings)
Candidates (scope ONE with the user first): rich-text toolbar Slice 3 (stroke/Tc/Tz on Arabic overlay, RTL direction-aware controls, per-run rich text); DOCX editor features; Arabic-RTL follow-ups; true-edit F10–F16. See KNOWN_ISSUES.md + the per-feature ceilings in CLAUDE.md.
